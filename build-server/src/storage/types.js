/**
 * StorageAdapter interface (JSDoc-typed):
 *   putFile(key, readStream, contentType) => Promise<void>
 * Implementations must be safe for sequential awaits over many files.
 */
module.exports = {};
