# Subset of the root .env.example consumed by the reverse proxy.
# docker-compose passes these through from the root .env file.

PORT=8000
API_SERVER_URL=http://api:9000
SITE_DOMAIN=yok.ninja
ARTIFACT_BASE_URL=https://YOUR_BUCKET.s3.YOUR_REGION.amazonaws.com/__output

# --- shared secrets ---
PROXY_SERVICE_TOKEN=
